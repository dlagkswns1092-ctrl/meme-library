spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/meme_db
    username: postgres
    password: Chos1234!

  mvc:
    hiddenmethod:
      filter:
        enabled: true

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  server:
    port: 8080